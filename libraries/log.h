#ifndef LOG_H
#define LOG_H

class loggin{
    public:
        bool loggin::CheckLogFile(); //function returns if logfile exists or doesnt
    private:
        bool log_file; // var that tells us if the log file exists 
};

#endif